# Оборудыш — статус разработки (24.07.2026)

- Автоматическая регистрация выполняется по Telegram-нику сразу после согласия с регламентом; повторные строки одного ника объединяются.

## Обновление 18.07.2026

- Профиль показывает отделы перед организациями и сворачивает длинный список до двух строк.
- Сняты запреты на срочные/воскресные заявки и получасовые слоты; доступно любое время с точностью до минуты.
- Проверка занятости переведена на точные интервалы, лимит 50 разных позиций удалён.
- Файловые выгрузки заменены постоянными листами `Заявки` и `626` в Google Sheets.
- 29 unit-тестов, Python/JS syntax — PASS.


## Обновление 21.07.2026

- Добавлена командная инвентаризация: запуск старшим, выбор участников, параллельная работа с атомарным закреплением категорий и продолжение после повторного входа.
- Сохраняются наличие, состояние, источник, участие в рентале, место хранения, данные о выдаче, комментарий и проверивший.
- На время события сервер блокирует создание/изменение заявок и выдачу оборудования.
- При запуске и завершении формируются листы `Инвентарка — ДАТА` и `Итоги рентал — ДАТА`; старшие получают итоговую сводку.
- Добавлены справочник мест хранения, штатное и экстренное завершение, новые экраны и адаптивное оформление.


## Обновление 17.07.2026

- Актуальный каталог загружен из опубликованной таблицы: 153 уникальные позиции
  и 336 допустимых номеров экземпляров.
- Закрытый лист `люди` подключён к регистрации.
- Рассылка 97 адресатам старого бота реализована через `/migrateold` с паролем; новая БД не изменяется.
- Локальная очередь баллов принимает ФИО из листа `люди`.

## Готово

- Полный цикл заявок на оборудование и бронирований 626.
- Панели администратора и старшего, верификация, чёрный список, категории, каталог и рассылки.
- Фото сдачи без хранения на сервере, проблемные возвраты, чаты и непрочитанные сообщения.
- Паспорта экземпляров с состоянием и историей; неисправные экземпляры исключаются из выдачи.
- Недельный календарь задач команды.
- SSE-обновления с `/api/revision` и резервным polling.
- Напоминания, автоотмены, ежедневная и месячная статистика, резервные копии.
- Локальная очередь баллов и опциональная синхронизация Google Sheets.
- Production временно скрыт через `ENABLE_PRODUCTION_ROLE=0`.
- Локальные шрифты Montserrat и кэширование версионированной статики.

## Проверено локально

- Unit-тесты — PASS, включая точные интервалы, инвентаризацию и конфликт захвата категории.
- Полный DEV E2E: регистрация → заявка → куратор → согласование → выдача с номером → фото сдачи → закрытие.
- Полный DEV E2E брони 626.
- Паспорт экземпляра и история выдачи/возврата.
- SSE `ready/change`, fallback-логика, Python/JS syntax.
- Загрузка HTML/CSS/JS/woff2 и cache headers.

## Осталось сделать дома

1. Задать `INVENTORY_SOURCE_SHEET_ID`/`INVENTORY_SOURCE_SHEET_TAB` и дать Service Account доступ к исходному инвентарному листу.
2. Создать отдельную Google-таблицу для баллов и включить уже готовую синхронизацию.
3. Актуализировать список и количество оборудования в каталоге.
4. Прогнать финальные локальные и Telegram-тесты.
5. После проверки обновить единственный production-сервис по `DEPLOY.md` с резервной копией БД.

Подробный порядок продолжения записан в `NEXT.md`.

QR-сканер и рейтинг порядочности не входят в текущий релиз.


## Обновление 07.09.2026

Исправлены повторная выдача невозвращённых экземпляров, расчёт одновременной занятости, точное время выбора номеров и сохранение исходных названий. Сдача оборудования и 626 подтверждается после доставки фото; при ошибке фотографии остаются в форме для повтора.

Добавлены восстановление черновика, частичное добавление избранного, подсказки следующего действия и читаемые карточки. Владелец может отменить 626 только до начала; после начала — сдача с фото или отмена старшим с причиной. Куратор 626 может освободить бронь для нового куратора, старший тоже может инициировать смену.

Заявка без куратора через 36 часов отправляется в личные сообщения всех администраторов с датой/временем выдачи, полным составом, сроком и кнопками ответа. На ответ даётся 6 часов. После этого не ответившие считаются проигнорировавшими, их поздние ответы не назначают куратора, а в канал приходит предложение отклонить заявку с упоминанием старших. Команда `/offerstatus` показывает старшим количество отказов, игнорирований, ожидающих ответов и недоставленных сообщений. Старший может отклонить заявку до выдачи с обязательной причиной.

Для выкладки заменить `bot/main.py`, `prototype/index.html`, `prototype/style.css` и актуальный `prototype/catalog.js`. Новых переменных `.env` нет; используются существующие `ADMIN_IDS`, `SENIOR_ADMIN_IDS`, `ADMIN_CHAT_ID` и добавленные командой `/addadmin` админы. Сделать обычный бэкап рабочей БД и перезапустить единственный сервис: новые таблицы доставки/ответов создаются автоматически. Рабочую БД не удалять.

После выкладки проверить в Telegram фото оборудования и 626 (в том числе ошибку и повтор), смену куратора, личные приглашения и ответы администраторов, упоминание старших в канале. Проверки разработки используют временную БД и имитацию Telegram; реальная доставка ими не подтверждается.
